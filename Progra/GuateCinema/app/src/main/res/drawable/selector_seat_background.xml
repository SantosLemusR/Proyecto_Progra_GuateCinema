<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado seleccionado -->
    <item android:state_selected="true" android:drawable="@color/seat_selected_color" />
    <!-- Estado por defecto -->
    <item android:drawable="@color/seat_default_color" />
</selector>
